Review the agent architecture in this portfolio project (`portfolio/backend/`).

Read `main.py`, `rag.py`, `agent.py`, and `mcp_server.py` (whichever exist). Then:

1. **Describe the current flow** — how a visitor question moves from the API endpoint through to a response
2. **Identify the weakest point** — where is the answer quality most likely to degrade?
3. **Suggest exactly one improvement** — the single change with the best effort-to-impact ratio

The improvement should be something that can be implemented in under 2 hours and makes the portfolio demonstrably more impressive. Don't suggest things that need paid APIs or complex infrastructure.

Focus on: retrieval quality, answer grounding, streaming behaviour, or agent tool-use patterns.
